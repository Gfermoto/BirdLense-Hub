# x86/Docker only
ultralytics==8.4.33
# ByteTrack / BoT-SORT: без lap у boxes.id часто None — нестабильные тесты и пустая детекция.
lap>=0.5.12
pyyaml==6.0.3
pydantic>=2.6,<3
paho-mqtt==2.1.0
requests==2.33.0
paramiko>=5.0.0,<6

# numpy 2.x — совместим с opencv, librosa, ultralytics
numpy>=2.0,<3
librosa==0.11.0
matplotlib==3.8.4

# OpenVINO runtime для processor.inference_backend=openvino (Ultralytics IR).
openvino>=2024.0
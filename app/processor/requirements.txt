# x86/Docker only
ultralytics==8.4.33
# Trapper / custom YOLO .pt checkpoints may pickle with dill (torch backend).
dill>=0.3.8,<1
# ByteTrack / BoT-SORT: без lap у boxes.id часто None — нестабильные тесты и пустая детекция.
lap>=0.5.12
pyyaml==6.0.3
pydantic>=2.6,<3
paho-mqtt==2.1.0
requests==2.33.0
paramiko>=4,<5

# numpy 2.x — совместим с opencv, ultralytics
numpy>=2.0,<3

# EfficientNetB2 species classifier runtime (HF preprocessing + ONNX/OpenVINO bundles).
transformers>=4.40,<5
timm>=0.9,<2
onnxruntime>=1.17,<2
safetensors>=0.4

# EfficientNetB2 species classifier (dennisjooo/Birds-Classifier-EfficientNetB2).
transformers>=4.40,<5
timm>=0.9,<2
onnxruntime>=1.17,<2
safetensors>=0.4

# Birder EU-common 707-species classifier (processor.classifier_engine=birder_eu).
birder>=0.5.2,<0.6
# Behavior v1 OpenVINO (video tracklet RGB)

- `behavior_video_model.xml` / `.bin` — IR FP16, input `X` shape `[1, 192]` (8×8×3 mean RGB).
- `behavior_video_export.json` — labels + coef/intercept (`behavior_video_export@v1`).

Regenerate: `docs/ml/BEHAVIOR_MODEL.md`.

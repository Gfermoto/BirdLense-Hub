# Pending changes requiring environment/config approval

No files were deleted and no live environment configuration was changed.

Recommended production environment changes (apply on server/VPS, not in repo):

```bash
BIRDLENSE_ENV=production
BIRDLENSE_STRICT_API_AUTH=1
FLASK_SECRET_KEY=<strong-random>
PROCESSOR_SECRET=<strong-random>
BIRDLENSE_UI_API_KEY=<strong-random-for-automation-if-needed>
MCP_TOKEN=<strong-random-if-mcp-enabled>
TRUSTED_PROXY=1  # only if Gunicorn is reachable only through nginx/reverse proxy
```

Reverse proxy / TLS:
- enforce HTTPS redirect at public edge;
- never expose Gunicorn directly to the internet;
- keep `CORS_ORIGINS` explicit, never `*` with credentials.

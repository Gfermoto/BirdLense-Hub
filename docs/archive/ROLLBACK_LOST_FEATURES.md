# Что потеряли при откате (до GPU) и стоит ли возвращать

**Архив.** Все фазы 0–5 реализованы (март 2026). Документ сохранён для истории.

---

После отката к коммиту 5f108c4 (до Intel VA-API) из кодовой базы исчезли не только GPU-фичи, но и часть правок. В этом документе: что именно **потеряли** (и как вернуть), что **не потеряно**, пошаговые рекомендации по Intel GPU по образцу Frigate.

**Итог:** все пункты плана (0–7, 5.1, фазы 0–5) реализованы. Prometheus `/api/metrics` добавлен.

См. [TROUBLESHOOTING.md](../TROUBLESHOOTING.md) для актуальной диагностики Intel GPU.
